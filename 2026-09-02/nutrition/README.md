Nutrition hosted proofs for 2026-09-02 (PR https://github.com/Harding-Ventures-LLC/insp1re/pull/5).

Reused the existing `journey.proof` session. No new Auth signups and no confirmation emails.

Shots are Flutter web release at 390pt × 844pt (2x). The plate is a synthetic green PNG used only for capture — not committed in the app repo.

`parse-sheet-*.png` is the honest fail-closed sheet (`OPENAI_API_KEY` still unset). `parse-sheet-ready-*.png` is the designed insight after a SQL-only status update on that same hosted row.
